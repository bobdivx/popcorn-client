name: WebOS Build (IPK)

on:
  push:
    branches:
      - main
      - master
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
      - master
  workflow_dispatch:  # Permet de déclencher manuellement depuis l'interface GitHub

jobs:
  # Job 1: Build frontend Astro
  build-frontend:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Astro frontend
        run: npm run build

      - name: Upload frontend artifact
        uses: actions/upload-artifact@v4
        with:
          name: popcorn-frontend-dist
          path: dist/
          retention-days: 7

  # Job 2: Prepare WebOS structure
  prepare-webos:
    needs: build-frontend
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download frontend artifact
        uses: actions/download-artifact@v4
        with:
          name: popcorn-frontend-dist
          path: dist/

      - name: Get version
        id: version
        run: |
          VERSION=$(jq -r '.client.version' VERSION.json)
          echo "version=${VERSION}" >> $GITHUB_OUTPUT
          echo "Version: ${VERSION}"

      - name: Prepare WebOS structure
        run: |
          # Créer le dossier frontend dans webos/
          mkdir -p webos/frontend
          
          # Copier le frontend buildé
          cp -r dist/* webos/frontend/
          
          # Copier les icônes
          cp src-tauri/icons/icon.png webos/icon.png
          cp src-tauri/icons/icon.png webos/icon-large.png
          
          # Mettre à jour appinfo.json avec la version
          jq --arg version "${{ steps.version.outputs.version }}" '.version = $version' webos/appinfo.json > webos/appinfo.json.tmp
          mv webos/appinfo.json.tmp webos/appinfo.json
          
          echo "✅ Structure WebOS préparée"

      - name: Upload WebOS structure artifact
        uses: actions/upload-artifact@v4
        with:
          name: popcorn-webos-structure
          path: webos/
          retention-days: 7

  # Job 3: Build IPK with ares-package
  build-ipk:
    needs: prepare-webos
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download WebOS structure artifact
        uses: actions/download-artifact@v4
        with:
          name: popcorn-webos-structure
          path: webos/

      - name: Build IPK with Docker
        run: |
          # Utiliser une image Docker avec WebOS SDK
          # Image recommandée: oddstr13/docker-tizen-webos-sdk ou similaire
          docker run --rm \
            -v "${{ github.workspace }}/webos:/workspace" \
            -w /workspace \
            ghcr.io/oddstr13/docker-tizen-webos-sdk:latest \
            ares-package . || {
              echo "⚠️ Build avec Docker a échoué, tentative avec ares-cli via npm..."
              
              # Fallback: essayer avec ares-cli via npm
              npm install -g ares-cli || {
                echo "❌ Impossible d'installer ares-cli"
                echo "Le build IPK nécessite WebOS SDK (Docker ou installation native)"
                exit 1
              }
              
              cd webos
              ares-package .
            }

      - name: Find IPK file
        id: find-ipk
        run: |
          IPK_FILE=$(find webos -name "*.ipk" -type f | head -n 1)
          if [ -z "$IPK_FILE" ]; then
            echo "❌ Aucun fichier IPK trouvé!"
            exit 1
          fi
          
          IPK_NAME=$(basename "$IPK_FILE")
          echo "path=$IPK_FILE" >> $GITHUB_OUTPUT
          echo "name=$IPK_NAME" >> $GITHUB_OUTPUT
          echo "✅ IPK trouvé: $IPK_NAME"

      - name: Upload IPK artifact
        uses: actions/upload-artifact@v4
        with:
          name: popcorn-webos-ipk
          path: ${{ steps.find-ipk.outputs.path }}
          retention-days: 90

  # Job 4: Create GitHub Release
  create-release:
    needs: [build-ipk]
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master' || startsWith(github.ref, 'refs/tags/v'))
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Get version
        id: version
        run: |
          VERSION=$(jq -r '.client.version' VERSION.json)
          echo "version=${VERSION}" >> $GITHUB_OUTPUT
          echo "Version: ${VERSION}"

      - name: Determine release tag
        id: tag
        run: |
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            TAG="${{ github.ref_name }}"
          else
            TAG="v${{ steps.version.outputs.version }}"
          fi
          echo "tag=${TAG}" >> $GITHUB_OUTPUT
          echo "Release tag: ${TAG}"

      - name: Download IPK artifact
        uses: actions/download-artifact@v4
        with:
          name: popcorn-webos-ipk
          path: ./artifacts

      - name: Install GitHub CLI
        run: |
          type -p curl >/dev/null || (apt update && apt install curl -y)
          curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg \
          && chmod go+r /usr/share/keyrings/githubcli-archive-keyring.gpg \
          && echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | tee /etc/apt/sources.list.d/github-cli.list > /dev/null \
          && apt update \
          && apt install gh -y

      - name: Check if release exists
        id: check-release
        run: |
          TAG="${{ steps.tag.outputs.tag }}"
          if gh release view "$TAG" &>/dev/null; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "Release $TAG already exists, will update it"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "Release $TAG does not exist, will create it"
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create or update GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: Release ${{ steps.tag.outputs.tag }} - WebOS
          body: |
            ## WebOS Release ${{ steps.tag.outputs.tag }}

            ### Artifacts
            - IPK pour installation sur téléviseurs LG WebOS

            ### Installation
            1. Téléchargez le fichier `.ipk`
            2. Installez-le sur votre TV WebOS via Developer Mode ou via l'application Homebrew Channel

            ### Commit
            ${{ github.sha }}

            ### Workflow
            [View workflow run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
          files: ./artifacts/**
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
