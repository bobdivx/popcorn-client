---
import '../../styles/global.css';

// Route dynamique /player/[slug] pour éviter 404 en dev (et en prod si le serveur sert cette page pour /player/*).
// En build static (format: file), ne pas utiliser slug "index" : ça génère /player/index et entre en conflit avec player.astro.
export function getStaticPaths() {
  return [{ params: { slug: '_' } }];
}
---
<!doctype html>
<html lang="fr" data-theme="dark" class="overflow-x-hidden">
  <head>
    <meta charset="UTF-8" />
    <script is:inline>
      (function(){try{var s=localStorage.getItem('popcorn_client_user_preferences');var t='dark';if(s){var p=JSON.parse(s);t=p.theme||'auto';}if(t==='auto'){var h=new Date().getHours();t=(h>=7&&h<20)?'light':'dark';}document.documentElement.dataset.theme=t;}catch(e){}})();
    </script>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Redirection… - Popcornn</title>
  </head>
  <body class="min-h-screen bg-page text-white flex items-center justify-center">
    <div class="text-center">
      <p class="text-white/80">Redirection…</p>
    </div>

    <script is:inline>
      (function () {
        try {
          var pathname = window.location.pathname || '';
          var m = pathname.match(/^\/player\/(.+)$/);
          var id = (m && m[1] && m[1] !== '_' && m[1] !== 'index') ? m[1] : null;
          if (!id) {
            var params = new URLSearchParams(window.location.search);
            id = params.get('contentId') || params.get('id') || params.get('slug');
          }
          if (id) {
            window.location.replace('/torrents?slug=' + encodeURIComponent(id));
            return;
          }
          window.location.replace('/dashboard');
        } catch (e) {
          window.location.replace('/dashboard');
        }
      })();
    </script>
  </body>
</html>
