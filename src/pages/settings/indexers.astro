---
import SettingsLayout from '../../layouts/SettingsLayout.astro';
import IndexersPageContent from '../../components/settings/IndexersPageContent';
import PermissionGuard from '../../components/ui/PermissionGuard';
import DsPageHeader from '../../components/ui/DsPageHeader';
---

<SettingsLayout title="Gestion des Indexers - Popcorn Client">
  <div class="ds-container max-w-5xl py-4 sm:py-6 px-3 sm:px-6">
    <DsPageHeader
      client:only="preact"
      titleKey="settingsPages.indexers.title"
      subtitleKey="settingsPages.indexers.subtitle"
    />
    <PermissionGuard client:only="preact" permission="settings.indexers">
      <IndexersPageContent client:only="preact" />
    </PermissionGuard>
  </div>
</SettingsLayout>
